# ✅ Чеклист прав для роли "Member"

## 🎯 Откройте в Strapi Admin:
**Settings → Users & Permissions Plugin → Roles → Member**

---

## 📋 Что нужно отметить (галочки):

### 1. **Users-permissions** (раздел)
   - [ ] **AUTH** → `register` (POST) ✅
   - [ ] **AUTH** → `forgotPassword` (POST) ✅
   - [ ] **AUTH** → `resetPassword` (POST) ✅
   - [ ] **USER** → `me` (GET) ✅

### 2. **Project** (раздел)
   - [ ] `find` (GET) ✅
   - [ ] `findOne` (GET) ✅
   - [ ] `update` (PUT) ✅ **← ВАЖНО! Для редактирования описания проекта**

### 3. **Task** (раздел)
   - [ ] `find` (GET) ✅
   - [ ] `findOne` (GET) ✅
   - [ ] `create` (POST) ✅ **← ВАЖНО! Для создания задач/подзадач**
   - [ ] `update` (PUT) ✅ **← ВАЖНО! Для изменения статуса задач**

### 4. **Meeting-note** (раздел)
   - [ ] `find` (GET) ✅
   - [ ] `findOne` (GET) ✅
   - [ ] `create` (POST) ✅
   - [ ] `update` (PUT) ✅

### 5. **Department** (раздел)
   - [ ] `find` (GET) ✅
   - [ ] `findOne` (GET) ✅

### 6. **Board-stage** (раздел)
   - [ ] `find` (GET) ✅
   - [ ] `findOne` (GET) ✅

### 7. **Project-document** (раздел)
   - [ ] `find` (GET) ✅
   - [ ] `findOne` (GET) ✅
   - [ ] `create` (POST) ✅ **← ВАЖНО! Для прикрепления документов**

### 8. **Project-survey** (раздел)
   - [ ] `find` (GET) ✅
   - [ ] `findOne` (GET) ✅
   - [ ] `create` (POST) ✅ **← ВАЖНО! Для создания анкет**

### 9. **Analytics** (раздел)
   - [ ] `summary` (GET) ✅ (если нужна аналитика)

---

## ❌ Что НЕ нужно отмечать:

- ❌ Project → `create`, `delete`
- ❌ Task → `delete`
- ❌ Meeting-note → `delete`
- ❌ Project-document → `update`, `delete`
- ❌ Project-survey → `update`, `delete`
- ❌ Department → `create`, `update`, `delete`
- ❌ Board-stage → `create`, `update`, `delete`
- ❌ Admin-users → все
- ❌ Activity-log → все (обычно не нужно)

---

## 🔍 После настройки проверьте:

1. ✅ Пользователь может войти в систему
2. ✅ Пользователь может создать задачу
3. ✅ Пользователь может изменить статус задачи
4. ✅ Пользователь может редактировать описание проекта
5. ✅ Пользователь может загрузить документ
6. ✅ Пользователь может создать анкету
7. ✅ Пользователь может создать заметку

---

## ⚠️ Если что-то не работает:

1. **Убедитесь, что пользователь имеет роль "Member"** (не "Authenticated")
2. **Перезапустите сервер Strapi** после изменения прав
3. **Проверьте, что пользователь не заблокирован** (`blocked: false`)
4. **Проверьте, что пользователь подтвержден** (`confirmed: true`)

---

## 📝 Быстрая настройка:

1. Откройте: **Settings → Users & Permissions → Roles → Member**
2. Прокрутите до каждого раздела
3. Отметьте указанные выше права
4. Нажмите **Save** внизу страницы
5. Перезапустите сервер Strapi
