# Руководство по настройке прав доступа (Permissions) в Strapi

## 📋 Общие понятия

В Strapi каждая роль имеет набор прав (permissions) для доступа к API endpoints. Каждое право соответствует определенному действию (action) над ресурсом.

### Структура прав:
- **Format**: `api::[collection-name].[collection-name].[action]`
- **Пример**: `api::project.project.find` - право на получение списка проектов

### Типы действий (Actions):
- **find** - получить список записей (GET /api/projects)
- **findOne** - получить одну запись (GET /api/projects/:id)
- **create** - создать запись (POST /api/projects)
- **update** - обновить запись (PUT /api/projects/:id)
- **delete** - удалить запись (DELETE /api/projects/:id)

---

## 🔐 Роль: **Authenticated**

**Важно:** Это стандартная роль Strapi, которая автоматически присваивается всем авторизованным пользователям.

### Назначение:
- Базовая роль для всех, кто может войти в систему
- Минимальные права для авторизации
- Обычно используется как "fallback" для всех авторизованных

### ✅ Обязательные права для авторизации:

### ✅ Обязательные права для авторизации:

#### 1. **Auth (Авторизация)**
- ✅ `auth.local` (POST) - **Вход в систему** - ОБЯЗАТЕЛЬНО!
- ✅ `auth.local.register` (POST) - Регистрация (если нужна)
- ✅ `auth.forgot-password` (POST) - Восстановление пароля
- ✅ `auth.reset-password` (POST) - Сброс пароля

#### 2. **User (Пользователь)**
- ✅ `user.me` (GET) - Получить информацию о текущем пользователе - ОБЯЗАТЕЛЬНО!

### ✅ Права для просмотра данных:

#### 3. **Project (Проекты)**
- ✅ `project.find` (GET) - Просмотр списка проектов
- ✅ `project.findOne` (GET) - Просмотр одного проекта

#### 4. **Task (Задачи)**
- ✅ `task.find` (GET) - Просмотр списка задач
- ✅ `task.findOne` (GET) - Просмотр одной задачи

#### 5. **Meeting Note (Заметки совещаний)**
- ✅ `meeting-note.find` (GET) - Просмотр заметок
- ✅ `meeting-note.findOne` (GET) - Просмотр одной заметки

#### 6. **Department (Отделы)**
- ✅ `department.find` (GET) - Просмотр отделов
- ✅ `department.findOne` (GET) - Просмотр одного отдела

#### 7. **Board Stage (Стадии канбана)**
- ✅ `board-stage.find` (GET) - Просмотр стадий
- ✅ `board-stage.findOne` (GET) - Просмотр одной стадии

#### 8. **Project Document (Документы проекта)**
- ✅ `project-document.find` (GET) - Просмотр документов
- ✅ `project-document.findOne` (GET) - Просмотр одного документа

#### 9. **Project Survey (Анкеты)**
- ✅ `project-survey.find` (GET) - Просмотр анкет
- ✅ `project-survey.findOne` (GET) - Просмотр одной анкеты

#### 10. **Analytics (Аналитика)**
- ✅ `analytics.summary` (GET) - Просмотр аналитики (если нужна)

### ⚠️ Права для создания/изменения (по необходимости):

Если обычные пользователи должны создавать/редактировать:

#### **Task (Задачи)**
- ⚠️ `task.create` (POST) - Создать задачу
- ⚠️ `task.update` (PUT) - Обновить задачу (изменить статус)
- ❌ `task.delete` (DELETE) - Удалить задачу (обычно только для руководителей)

#### **Meeting Note (Заметки)**
- ⚠️ `meeting-note.create` (POST) - Создать заметку
- ⚠️ `meeting-note.update` (PUT) - Обновить свою заметку
- ❌ `meeting-note.delete` (DELETE) - Удалить заметку

---

## 👤 Роль: **Member**

**Важно:** Это ваша кастомная роль для обычных пользователей (членов команды).

### Назначение:
- Роль для обычных сотрудников
- Могут просматривать проекты, задачи, заметки
- Могут создавать задачи и заметки (если настроено)
- НЕ могут редактировать проекты

### ✅ Обязательные права для авторизации:

#### 1. **Auth (Авторизация)**
- ✅ `auth.local` (POST) - **Вход в систему** - ОБЯЗАТЕЛЬНО!
- ✅ `auth.local.register` (POST) - Регистрация (если нужна)
- ✅ `auth.forgot-password` (POST) - Восстановление пароля
- ✅ `auth.reset-password` (POST) - Сброс пароля

#### 2. **User (Пользователь)**
- ✅ `user.me` (GET) - Получить информацию о текущем пользователе - ОБЯЗАТЕЛЬНО!

### ✅ Права для просмотра данных:

#### 3. **Project (Проекты)**
- ✅ `project.find` (GET) - Просмотр списка проектов
- ✅ `project.findOne` (GET) - Просмотр одного проекта
- ⚠️ `project.update` (PUT) - **Редактировать описание проекта** - ОТМЕТЬТЕ ЭТО!
- ❌ `project.create` - НЕ может создавать проекты
- ❌ `project.delete` - НЕ может удалять проекты

#### 4. **Task (Задачи)**
- ✅ `task.find` (GET) - Просмотр списка задач
- ✅ `task.findOne` (GET) - Просмотр одной задачи
- ✅ `task.create` (POST) - Создать задачу (если нужно)
- ✅ `task.update` (PUT) - Обновить задачу (изменить статус)
- ❌ `task.delete` (DELETE) - НЕ может удалять задачи

#### 5. **Meeting Note (Заметки совещаний)**
- ✅ `meeting-note.find` (GET) - Просмотр заметок
- ✅ `meeting-note.findOne` (GET) - Просмотр одной заметки
- ✅ `meeting-note.create` (POST) - Создать заметку
- ⚠️ `meeting-note.update` (PUT) - Обновить свою заметку (если нужно)
- ❌ `meeting-note.delete` (DELETE) - НЕ может удалять заметки

#### 6. **Department (Отделы)**
- ✅ `department.find` (GET) - Просмотр отделов
- ✅ `department.findOne` (GET) - Просмотр одного отдела
- ❌ `department.create` - НЕ может создавать отделы
- ❌ `department.update` - НЕ может редактировать отделы
- ❌ `department.delete` - НЕ может удалять отделы

#### 7. **Board Stage (Стадии канбана)**
- ✅ `board-stage.find` (GET) - Просмотр стадий
- ✅ `board-stage.findOne` (GET) - Просмотр одной стадии
- ❌ `board-stage.create` - НЕ может создавать стадии
- ❌ `board-stage.update` - НЕ может редактировать стадии
- ❌ `board-stage.delete` - НЕ может удалять стадии

#### 8. **Project Document (Документы проекта)**
- ✅ `project-document.find` (GET) - Просмотр документов
- ✅ `project-document.findOne` (GET) - Просмотр одного документа
- ✅ `project-document.create` (POST) - **Загружать документы** - ОТМЕТЬТЕ ЭТО!
- ❌ `project-document.update` - НЕ может обновлять документы
- ❌ `project-document.delete` - НЕ может удалять документы

#### 9. **Project Survey (Анкеты)**
- ✅ `project-survey.find` (GET) - Просмотр анкет
- ✅ `project-survey.findOne` (GET) - Просмотр одной анкеты
- ✅ `project-survey.create` (POST) - **Создавать анкеты** - ОТМЕТЬТЕ ЭТО!
- ⚠️ `project-survey.update` (PUT) - Редактировать анкеты (если нужно)
- ❌ `project-survey.delete` - НЕ может удалять анкеты

#### 10. **Analytics (Аналитика)**
- ✅ `analytics.summary` (GET) - Просмотр аналитики (если нужна)

---

## 👔 Роль: **Lead** (Руководитель)

Руководители имеют все права обычных пользователей + дополнительные права на редактирование.

### ✅ Все права из роли Authenticated + дополнительно:

#### **Project (Проекты)**
- ✅ `project.create` (POST) - Создать проект
- ✅ `project.update` (PUT) - Редактировать проект
- ✅ `project.delete` (DELETE) - Удалить проект

#### **Task (Задачи)**
- ✅ `task.create` (POST) - Создать задачу
- ✅ `task.update` (PUT) - Редактировать задачу
- ✅ `task.delete` (DELETE) - Удалить задачу

#### **Meeting Note (Заметки)**
- ✅ `meeting-note.create` (POST) - Создать заметку
- ✅ `meeting-note.update` (PUT) - Редактировать заметку
- ✅ `meeting-note.delete` (DELETE) - Удалить заметку

#### **Project Document (Документы)**
- ✅ `project-document.create` (POST) - Загрузить документ
- ✅ `project-document.update` (PUT) - Обновить документ
- ✅ `project-document.delete` (DELETE) - Удалить документ

#### **Project Survey (Анкеты)**
- ✅ `project-survey.create` (POST) - Создать анкету
- ✅ `project-survey.update` (PUT) - Редактировать анкету
- ✅ `project-survey.delete` (DELETE) - Удалить анкету

#### **Analytics (Аналитика)**
- ✅ `analytics.summary` (GET) - Полная аналитика

---

## 👑 Роль: **Admin** (Администратор)

Администраторы имеют все права + управление пользователями.

### ✅ Все права из роли Lead + дополнительно:

#### **Admin Users (Управление пользователями)**
- ✅ `admin-users.find` (GET) - Список пользователей
- ✅ `admin-users.findOne` (GET) - Информация о пользователе
- ✅ `admin-users.create` (POST) - Создать пользователя
- ✅ `admin-users.update` (PUT) - Обновить пользователя
- ✅ `admin-users.delete` (DELETE) - Удалить пользователя
- ✅ `admin-users.resetPassword` (POST) - Сбросить пароль

#### **Department (Отделы)**
- ✅ `department.create` (POST) - Создать отдел
- ✅ `department.update` (PUT) - Обновить отдел
- ✅ `department.delete` (DELETE) - Удалить отдел

#### **Board Stage (Стадии)**
- ✅ `board-stage.create` (POST) - Создать стадию
- ✅ `board-stage.update` (PUT) - Обновить стадию
- ✅ `board-stage.delete` (DELETE) - Удалить стадию

---

## 🚫 Роль: **Public** (Публичная)

Для неавторизованных пользователей (если нужен публичный доступ).

### ✅ Минимальные права:

#### **Project Survey (Публичные анкеты)**
- ✅ `project-survey.findByToken` (GET) - Просмотр публичной анкеты по токену
- ✅ `project-survey.submitResponse` (POST) - Отправка ответа на анкету

---

## 📝 Как настроить права в Strapi Admin:

1. Откройте админ-панель: `http://192.168.101.25:12005/admin`
2. Перейдите: **Settings** → **Users & Permissions Plugin** → **Roles**
3. Выберите роль (например, "Authenticated")
4. В разделе **Permissions** найдите нужный ресурс (например, "Project")
5. Отметьте нужные действия (find, findOne, create, update, delete)
6. Нажмите **Save**

---

## ⚡ Быстрая настройка для роли "Authenticated":

### Минимальный набор (только авторизация):
```
✅ Auth → local (POST)
✅ User → me (GET)
```

**Примечание:** Обычно роль "Authenticated" имеет только базовые права для авторизации. Основные права настраиваются для конкретных ролей (Member, Lead, Admin).

---

## ⚡ Быстрая настройка для роли "Member":

### Полный набор для роли "Member" (со всеми возможностями):

```
✅ Users-permissions:
   - AUTH → register (POST)
   - AUTH → forgotPassword (POST)
   - AUTH → resetPassword (POST)
   - USER → me (GET)

✅ Project:
   - find (GET)
   - findOne (GET)
   - update (PUT) ← ВАЖНО! Для редактирования описания

✅ Task:
   - find (GET)
   - findOne (GET)
   - create (POST) ← ВАЖНО! Для создания задач
   - update (PUT) ← ВАЖНО! Для изменения статуса задач

✅ Meeting Note:
   - find (GET)
   - findOne (GET)
   - create (POST)
   - update (PUT)

✅ Department:
   - find (GET)
   - findOne (GET)

✅ Board Stage:
   - find (GET)
   - findOne (GET)

✅ Project Document:
   - find (GET)
   - findOne (GET)
   - create (POST) ← ВАЖНО! Для загрузки документов

✅ Project Survey:
   - find (GET)
   - findOne (GET)
   - create (POST) ← ВАЖНО! Для создания анкет

✅ Analytics:
   - summary (GET) - если нужна аналитика
```

---

## 🔍 Проверка прав:

После настройки проверьте:
1. Пользователь может авторизоваться (`auth.local`)
2. Пользователь может получить свои данные (`user.me`)
3. Пользователь может просматривать проекты (`project.find`)
4. Пользователь может выполнять действия согласно своей роли

---

## ⚠️ Важные замечания:

1. **auth.local** - это самое важное право! Без него пользователь не сможет войти в систему.
2. **user.me** - необходимо для получения информации о текущем пользователе.
3. Права наследуются: Admin имеет все права Lead, Lead имеет все права Member.
4. Если пользователь не может выполнить действие - проверьте права его роли.

---

## 📊 Сравнительная таблица ролей:

| Действие | Authenticated | Member | Lead | Admin |
|----------|--------------|--------|------|-------|
| **Авторизация** |
| Вход в систему (auth.local) | ✅ | ✅ | ✅ | ✅ |
| Получить свои данные (user.me) | ✅ | ✅ | ✅ | ✅ |
| **Проекты** |
| Просмотр проектов | ⚠️ (если настроено) | ✅ | ✅ | ✅ |
| Создание проектов | ❌ | ❌ | ✅ | ✅ |
| Редактирование проектов | ❌ | ❌ | ✅ | ✅ |
| Удаление проектов | ❌ | ❌ | ✅ | ✅ |
| **Задачи** |
| Просмотр задач | ⚠️ (если настроено) | ✅ | ✅ | ✅ |
| Создание задач | ❌ | ✅ | ✅ | ✅ |
| Редактирование задач | ❌ | ✅ | ✅ | ✅ |
| Удаление задач | ❌ | ❌ | ✅ | ✅ |
| **Заметки совещаний** |
| Просмотр заметок | ⚠️ (если настроено) | ✅ | ✅ | ✅ |
| Создание заметок | ❌ | ✅ | ✅ | ✅ |
| Редактирование заметок | ❌ | ⚠️ (только свои) | ✅ | ✅ |
| Удаление заметок | ❌ | ❌ | ✅ | ✅ |
| **Документы** |
| Просмотр документов | ⚠️ (если настроено) | ✅ | ✅ | ✅ |
| Загрузка документов | ❌ | ❌ | ✅ | ✅ |
| Удаление документов | ❌ | ❌ | ✅ | ✅ |
| **Анкеты** |
| Просмотр анкет | ⚠️ (если настроено) | ✅ | ✅ | ✅ |
| Создание анкет | ❌ | ❌ | ✅ | ✅ |
| Управление анкетами | ❌ | ❌ | ✅ | ✅ |
| **Управление пользователями** |
| Просмотр пользователей | ❌ | ❌ | ❌ | ✅ |
| Создание пользователей | ❌ | ❌ | ❌ | ✅ |
| Управление пользователями | ❌ | ❌ | ❌ | ✅ |

**Легенда:**
- ✅ - Разрешено
- ❌ - Запрещено
- ⚠️ - Зависит от настроек (обычно не рекомендуется для Authenticated)

---

## 🎯 Рекомендации по настройке:

### Для роли "Authenticated":
- **Минимум:** Только `auth.local` и `user.me`
- **Назначение:** Базовая роль для всех авторизованных
- **Использование:** Обычно не используется напрямую, служит как fallback

### Для роли "Member":
- **Все права для просмотра** (find, findOne)
- **Права на создание задач и заметок** (create, update для task и meeting-note)
- **НЕ давать права на редактирование проектов**

### Для роли "Lead":
- **Все права Member +**
- **Права на редактирование проектов** (create, update, delete для project)
- **Права на управление документами и анкетами**

### Для роли "Admin":
- **Все права Lead +**
- **Права на управление пользователями** (admin-users.*)
- **Права на управление отделами и стадиями**
