# NNMC IT Project Board - Environment Variables Template
# Copy this file to .env and update values

# ============================================
# Backend (Strapi) Configuration
# ============================================

# Server
HOST=0.0.0.0
PORT=12005
NODE_ENV=production

# Security Keys (GENERATE YOUR OWN!)
# Use: openssl rand -base64 32
APP_KEYS=change-me-key1,change-me-key2,change-me-key3,change-me-key4
API_TOKEN_SALT=change-me-api-token-salt
ADMIN_JWT_SECRET=change-me-admin-jwt-secret
TRANSFER_TOKEN_SALT=change-me-transfer-token-salt
JWT_SECRET=change-me-jwt-secret

# Database
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# SMTP Configuration (for email verification & password reset)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@nnmc.kz
SMTP_REPLY_TO=support@nnmc.kz

# CORS - Frontend URL
FRONTEND_URL=http://192.168.101.25:13005

# ============================================
# Frontend Configuration
# ============================================

# API URL (for frontend build)
VITE_API_URL=http://192.168.101.25:12005

# Optional: public frontend URL (for share links)
VITE_FRONTEND_URL=http://192.168.101.25:13005

# KPI Timesheet module — kpiServer Strapi backend URL
# If not set, defaults to http://{hostname}:12007/api (works on localhost automatically)
# Set this in production / Coolify to the actual kpiServer backend address
VITE_KPI_API_BASE=http://192.168.101.25:12007/api

# ============================================
# Docker / Coolify Configuration
# ============================================

# These are used in docker-compose.yml
# Uncomment and set for production deployment

# COMPOSE_PROJECT_NAME=nnmc-it-board
