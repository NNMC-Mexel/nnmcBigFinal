# Система расчета KPI персонала клининга

Production-ready веб-система для автоматического расчёта KPI персонала клининга с полной историей изменений и аудитом.

## 🎯 Основные возможности

- ✅ Автоматический расчет KPI на основе табелей посещаемости
- ✅ Управление календарем и праздничными днями
- ✅ Полная история изменений (добавление, редактирование, удаление, восстановление)
- ✅ Гибкая система расчета для дневных и суточных графиков
- ✅ Экспорт отчетов в Excel (общий, для 1С, для бухгалтерии)
- ✅ Безопасная аутентификация с хешированием паролей
- ✅ Валидация данных и обработка ошибок
- ✅ Логирование всех операций

## 📋 Требования

- Python 3.8+
- Node.js 16+
- npm или yarn

## 🚀 Установка

### Backend

```bash
cd backend
pip install -r requirements.txt
```

### Frontend

```bash
cd frontend
npm install
```

## ⚙️ Конфигурация

### Backend

Создайте файл `.env` в папке `backend` (опционально):

```env
SECRET_KEY=your-secret-key-here
SESSION_TIMEOUT=3600
MAX_FILE_SIZE=10485760
LOG_LEVEL=INFO
```

Или используйте значения по умолчанию из `config.py`.

### Frontend

В файле `frontend/src/api.js` измените `API_BASE` если нужно:

```javascript
const API_BASE = "http://localhost:5000";
```

## 🏃 Запуск

### Backend

```bash
cd backend
python app_improved.py
```

Или используйте оригинальный `app.py`:

```bash
python app.py
```

### Frontend

```bash
cd frontend
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173` (или другому порту, указанному Vite).

## 📁 Структура проекта

```
kpi-timesheet-app/
├── backend/
│   ├── app.py                    # Оригинальное приложение
│   ├── app_improved.py           # Улучшенное приложение
│   ├── config.py                  # Конфигурация
│   ├── auth_utils.py              # Оригинальная аутентификация
│   ├── auth_utils_improved.py    # Улучшенная аутентификация
│   ├── calendar_manager.py        # Управление календарем
│   ├── kpi_calculator.py          # Оригинальный калькулятор
│   ├── kpi_calculator_improved.py # Улучшенный калькулятор
│   ├── kpi_storage.py             # Хранение данных
│   ├── timesheet_parser.py        # Парсер табелей
│   ├── utils.py                   # Утилиты
│   ├── requirements.txt           # Зависимости Python
│   └── data/                      # Данные (создается автоматически)
│       ├── KPIsum_dynamic.xlsx
│       ├── users.xlsx
│       ├── holidays.json
│       └── *.xlsx (логи)
├── frontend/
│   ├── src/
│   │   ├── App.jsx                # Главный компонент
│   │   ├── api.js                 # API клиент
│   │   └── components/
│   └── package.json
└── README.md
```

## 🔐 Безопасность

### Миграция паролей

Если вы используете старую версию с паролями в открытом виде, запустите миграцию:

```python
from auth_utils_improved import migrate_passwords
migrate_passwords()
```

Это автоматически захеширует все пароли в новом формате.

### Создание пользователя

По умолчанию создается пользователь:
- Логин: `admin-nnmc`
- Пароль: `123nnmc`
- Роль: `admin`

**⚠️ ВАЖНО:** Измените пароль после первого входа!

## 📊 API Endpoints

### Аутентификация

- `POST /api/login` - Вход в систему
- `GET /api/me` - Информация о текущем пользователе
- `POST /api/logout` - Выход

### Расчет KPI

- `POST /api/calc-kpi-json` - Расчет с возвратом JSON
- `POST /api/calc-kpi-excel` - Расчет с возвратом Excel
- `POST /api/calc-kpi-1c` - Расчет для экспорта в 1С
- `POST /api/calc-kpi-buh` - Расчет для бухгалтерии

### Календарь

- `GET /api/calendar/days?year=2025&month=12` - Дни месяца с типами
- `GET /api/calendar/holidays?year=2025&month=12` - Праздничные дни
- `POST /api/calendar/holidays` - Добавить праздничный день
- `DELETE /api/calendar/holidays?date=2025-12-16` - Удалить праздничный день

### Управление сотрудниками

- `GET /api/kpi-list` - Список сотрудников
- `POST /api/kpi-add` - Добавить сотрудника
- `POST /api/kpi-edit` - Редактировать сотрудника
- `POST /api/kpi-delete` - Удалить сотрудника
- `POST /api/kpi-restore` - Восстановить сотрудника

### Логи

- `GET /api/kpi-deleted-log` - Лог удалений
- `GET /api/kpi-edited-log` - Лог редактирований
- `GET /api/kpi-restored-log` - Лог восстановлений

## 🔧 Улучшения по сравнению с оригинальной версией

### Безопасность
- ✅ Хеширование паролей с солью
- ✅ Валидация размера и типа файлов
- ✅ Сессии с временем жизни
- ✅ Защита от основных уязвимостей

### Обработка ошибок
- ✅ Специфичные исключения
- ✅ Логирование всех ошибок
- ✅ Понятные сообщения пользователю

### Код
- ✅ Полная типизация (type hints)
- ✅ Dataclasses для структурированных данных
- ✅ Единый стиль кода
- ✅ Комментарии и документация

### Функциональность
- ✅ Управление календарем и праздниками
- ✅ API для работы с праздничными днями
- ✅ Улучшенная валидация данных
- ✅ Кэширование (готово к добавлению)

## 📝 Логика расчета KPI

### Дневные сотрудники
- Н.о (не отработано) = буквы в будни
- Факт дней = Назначено - Н.о (или из табеля, если указано)
- KPI = (Факт дней / Назначено) * KPI сумм

### Суточные сотрудники
- Н.о = буквы в будни + буквы в субботу
- Буквы в воскресенье/праздники не считаются
- KPI = (Отработано / Назначено) * KPI сумм

### Праздничные дни
- Праздники приравниваются к воскресенью
- Буквы на праздниках не считаются как Н.о
- Числа на праздниках считаются как отработанные дни

## 🐛 Решение проблем

### Ошибка "Файл не найден"
Убедитесь, что директория `backend/data` существует и доступна для записи.

### Ошибка авторизации
Проверьте, что файл `backend/data/users.xlsx` существует и содержит пользователей.

### Ошибка парсинга табеля
Убедитесь, что табель соответствует одному из поддерживаемых форматов:
- Казахский шаблон с заголовком "АТЫ-жөні (толығымен)"
- Простой формат с колонкой "Сотрудник"

## 📄 Лицензия

Внутренний проект для медицинской организации.

## 👥 Авторы

Проект создан на основе детального ТЗ для автоматизации расчета KPI персонала клининга.

---

**Версия:** 2.0 (улучшенная)  
**Дата:** 2025

